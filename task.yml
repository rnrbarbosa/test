---
platform: linux

resources:
  - name: controller
    type: git
    source:
      uri: https://github.com/rnrbarbosa/test.git
      branch: master

image_resource:
  type: docker-image
  source:
    repository: rnrbarbosa/terraform_alpine

run:
  path: sh
  args:
  - -exc
  - |
    find /
