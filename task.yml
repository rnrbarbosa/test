---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: rnrbarbosa/terraform_alpine

resources:
  - name: nuxeo-repo
    type: git
    source:
      uri: https://github.com/rnrbarbosa/test.git

run:
  path: sh
  args:
  - -exc
  - |
    find /
